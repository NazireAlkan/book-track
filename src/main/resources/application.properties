spring.application.name=booktrack

spring.main.banner-mode=off
#logging.level.root=ERROR

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:booktrack
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (Veritaban?n? web'de görüntülemek için)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

#admin bilgileri
#app.admin.email=${ADMIN_EMAIL:admin@booktrack.com}
#app.admin.password=${ADMIN_PASSWORD:password}
#app.admin.create-default=${CREATE_DEFAULT_ADMIN:false}

spring.redis.host=localhost
spring.redis.port=6379

